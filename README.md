The framework leverages various prompt-tuning paradigms (Hard, Soft, and Hybrid/Mix) and different code contexts (Before, After, and Diff) to enhance the performance of Just-In-Time (JIT) defect prediction.

---

## 1. Environment Setup

To run the experiments, please ensure you have Python 3.8+ and the following libraries installed:

```bash
pip install torch transformers scikit-learn pandas
